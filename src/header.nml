grf {
	grfid: "TT\05\01";
	name: string(STR_GRF_NAME);
	desc: string(STR_GRF_DESC);
	version: 4;
	min_compatible_version: 4;
}

cargotable {
	COAL, PASS
}

basecost { // Building, clearing, and maintaining canals costs 1/16 normal
	PR_BUILD_CANAL: 		-4;
	PR_CLEAR_CANAL:			-4;
	PR_BUILD_AQUEDUCT:		-4;
	PR_CLEAR_AQUEDUCT:		-4;
	PR_BUILD_LOCK:			-4;
	PR_CLEAR_LOCK:			-4;
	PR_MAINTENANCE_CANAL:	-4;
}